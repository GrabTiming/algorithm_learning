# 算法练习仓库

本仓库用于记录和练习算法题（主要为 C++ 实现）。结构清晰、按平台分类，适合刷题记录与复盘。

目录结构（简要说明）：

- `template.cpp` — 通用 C++ 模板
- `Atcoder/` — Atcoder 练习题代码（按赛题目录存放）
- `luogu/` — Luogu 练习题代码
- `nowcoder/` — 牛客网/练习题代码
- `output/` — （本地测试输出目录）

如何编译运行单个题目（示例）：

```bash
# 编译（C++17）
 g++ -std=c++17 -O2 -pipe -o solution path/to/file.cpp
# 运行
 ./solution < input.txt > output.txt
```

贡献与说明：

- 请在提交前确保代码可编译并通过样例测试。
- 若添加题目，请按平台目录放置对应 `.cpp` 文件，并在 `算法题分类.md` 中添加对应分类条目或在会话中告诉 assistant 更新分类。

自动化与会话更新：

- 已启用会话更新规则（在会话中可标记 “已完成: <file_path>”），assistant 会记录并提示把条目同步到 `算法题分类.md`。

---

如果需要，我可以为每个子目录生成单独的 `README` 或把每题的题目名/链接补充到 `算法题分类.md` 中。
